# 微服务架构设计

## 架构概述

本项目采用微服务架构，基于 Spring Cloud 生态构建。

## 技术栈

### 核心框架
- **Spring Boot**: 2.7.x
- **Spring Cloud**: 2021.x
- **Spring Cloud Alibaba**: 2021.x

### 服务治理
- **Nacos**: 服务注册与配置中心
- **Gateway**: API 网关
- **Sentinel**: 流量控制和服务降级

### 数据层
- **MySQL**: 主数据库
- **Redis**: 缓存和分布式锁
- **Elasticsearch**: 全文检索
- **MinIO**: 对象存储

### 消息队列
- **RocketMQ**: 异步消息

### 监控
- **Prometheus**: 指标收集
- **Grafana**: 可视化
- **SkyWalking**: 链路追踪

## 服务划分

### 用户服务 (system-service)
- 用户管理
- 角色权限
- 部门管理
- 菜单管理

### 业务服务 (business-service)
- 订单管理
- 产品管理
- 库存管理

### 文件服务 (file-service)
- 文件上传下载
- 图片处理
- CDN 集成

### 消息服务 (message-service)
- 站内消息
- 短信发送
- 邮件发送

## 服务通信

### 同步调用
```java
@FeignClient(name = "system-service", fallback = SystemServiceFallback.class)
public interface SystemServiceClient {
    
    @GetMapping("/api/user/{id}")
    Result<UserVO> getUserById(@PathVariable("id") Long id);
    
    @PostMapping("/api/user/batch")
    Result<List<UserVO>> getUserByIds(@RequestBody List<Long> ids);
}
```

### 异步消息
```java
// 发送消息
@Autowired
private RocketMQTemplate rocketMQTemplate;

public void createOrder(OrderDTO orderDTO) {
    // 创建订单
    Order order = orderService.create(orderDTO);
    
    // 发送消息
    rocketMQTemplate.convertAndSend("order-created", new OrderCreatedEvent(order.getId()));
}

// 消费消息
@Component
@RocketMQMessageListener(topic = "order-created", consumerGroup = "inventory-consumer")
public class InventoryConsumer implements RocketMQListener<OrderCreatedEvent> {
    
    @Override
    public void onMessage(OrderCreatedEvent event) {
        // 扣减库存
        inventoryService.deduct(event.getOrderId());
    }
}
```

## 网关设计

### 路由配置
```yaml
spring:
  cloud:
    gateway:
      routes:
        - id: system-service
          uri: lb://system-service
          predicates:
            - Path=/api/system/**
          filters:
            - StripPrefix=2
            
        - id: business-service
          uri: lb://business-service
          predicates:
            - Path=/api/business/**
          filters:
            - StripPrefix=2
```

### 统一鉴权
```java
@Component
public class AuthFilter implements GlobalFilter, Ordered {
    
    @Override
    public Mono<Void> filter(ServerWebExchange exchange, GatewayFilterChain chain) {
        String token = exchange.getRequest().getHeaders().getFirst("Authorization");
        
        if (StringUtils.isEmpty(token)) {
            return unauthorized(exchange);
        }
        
        // 验证 token
        Claims claims = JwtUtils.parseToken(token);
        if (claims == null) {
            return unauthorized(exchange);
        }
        
        // 将用户信息传递给下游服务
        ServerWebExchange newExchange = exchange.mutate()
            .request(r -> r.header("X-User-Id", claims.getSubject()))
            .build();
        
        return chain.filter(newExchange);
    }
    
    @Override
    public int getOrder() {
        return -100;
    }
}
```

## 服务降级

### Sentinel 配置
```java
@FeignClient(name = "system-service", fallbackFactory = SystemServiceFallbackFactory.class)
public interface SystemServiceClient {
    
    @GetMapping("/api/user/{id}")
    Result<UserVO> getUserById(@PathVariable("id") Long id);
}

@Component
public class SystemServiceFallbackFactory implements FallbackFactory<SystemServiceClient> {
    
    @Override
    public SystemServiceClient create(Throwable cause) {
        return new SystemServiceClient() {
            @Override
            public Result<UserVO> getUserById(Long id) {
                // 返回降级数据
                return Result.success(UserVO.builder()
                    .id(id)
                    .username("未知用户")
                    .build());
            }
        };
    }
}
```

## 分布式事务

### Seata AT 模式
```java
@Service
public class OrderServiceImpl implements OrderService {
    
    @GlobalTransactional
    public void createOrder(OrderDTO orderDTO) {
        // 1. 创建订单
        orderMapper.insert(order);
        
        // 2. 扣减库存
        inventoryService.deduct(orderDTO.getProductId(), orderDTO.getQuantity());
        
        // 3. 扣减账户余额
        accountService.deduct(orderDTO.getUserId(), orderDTO.getTotalAmount());
    }
}
```

### RocketMQ 事务消息
```java
@Service
public class OrderServiceImpl implements OrderService {
    
    @Autowired
    private RocketMQTemplate rocketMQTemplate;
    
    public void createOrder(OrderDTO orderDTO) {
        // 1. 发送半消息
        TransactionMQMessage msg = new TransactionMQMessage();
        msg.setOrderId(order.getId());
        
        rocketMQTemplate.sendMessageInTransaction(
            "order-topic",
            MessageBuilder.withPayload(msg).build(),
            null
        );
    }
    
    @RocketMQTransactionListener
    public class OrderTransactionListener implements RocketMQLocalTransactionListener {
        
        @Override
        public RocketMQLocalTransactionState executeLocalTransaction(Message msg, Object arg) {
            try {
                // 2. 执行本地事务
                orderMapper.insert(order);
                return RocketMQLocalTransactionState.COMMIT;
            } catch (Exception e) {
                return RocketMQLocalTransactionState.ROLLBACK;
            }
        }
        
        @Override
        public RocketMQLocalTransactionState checkLocalTransaction(Message msg) {
            // 3. 回查本地事务
            Long orderId = ((TransactionMQMessage) msg.getPayload()).getOrderId();
            Order order = orderMapper.selectById(orderId);
            return order != null ? 
                RocketMQLocalTransactionState.COMMIT : 
                RocketMQLocalTransactionState.ROLLBACK;
        }
    }
}
```

## 配置中心

### Nacos 配置
```yaml
spring:
  cloud:
    nacos:
      config:
        server-addr: nacos:8848
        namespace: dev
        group: DEFAULT_GROUP
        file-extension: yaml
```

### 动态配置
```java
@Component
@RefreshScope
public class BusinessConfig {
    
    @Value("${business.order.timeout:30}")
    private Integer orderTimeout;
    
    @Value("${business.order.max-retry:3}")
    private Integer maxRetry;
}
```

## 最佳实践

1. **服务拆分**：按业务领域拆分，保持服务独立性
2. **接口设计**：RESTful API，统一响应格式
3. **异常处理**：全局异常处理，友好的错误信息
4. **日志规范**：统一日志格式，链路追踪 ID
5. **配置管理**：使用配置中心，避免硬编码
6. **监控告警**：完善的监控和告警机制

## 总结

1. 选择合适的技术栈
2. 合理划分服务边界
3. 统一通信和鉴权
4. 实现服务降级和熔断
5. 处理分布式事务
6. 完善监控和告警
